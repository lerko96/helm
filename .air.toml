root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main ./cmd/helm"
  bin = "./tmp/main"
  include_ext = ["go"]
  exclude_dir = ["tmp", "web", "ui"]
  delay = 500

[log]
  time = false

[color]
  build = "yellow"
  runner = "green"
